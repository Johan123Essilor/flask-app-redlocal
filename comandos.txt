puerto 443
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes

Ejecutar app
sudo python app.py

Mas seguro 
sudo setcap 'cap_net_bind_service=+ep' $(which python3)
python app.py

<img src="{{ cliente.img }}" alt="{{ cliente.alt }}">

en puerto 8000
app.run(host='0.0.0.0', port=8000)

.\venv\Scripts\activate

pip install flask

py app.run




 1. Agrega todos los archivos al control de versiones:
git add .

 2. Crea un commit con un mensaje (por ejemplo: "Primer commit"):
 git commit -m "Primer commit"

3. Sube los cambios al repositorio remoto en GitHub:
git push origin master
Si estás usando una rama diferente a master (como main), cambia master por main.